language: scala
sudo: false

scala:
   - 2.11.6

jdk:
  - openjdk7

before_script: travis_retry sbt +update

script:
  - sbt clean coverage test &&
    sbt coverageAggregate
after_success:
  - sbt coveralls
