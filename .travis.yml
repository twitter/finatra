language: scala

scala:
  - 2.11

jdk:
  - oraclejdk7
  - openjdk6
  - openjdk7

before_script:
  # default $SBT_OPTS is irrelevant to sbt lancher
  - unset SBT_OPTS

script:
  - ./sbt clean coverage test &&
    ./sbt coverageAggregate
after_success:
  - ./sbt coveralls