junit_tests(
  name='scala',
  dependencies=[
    'finatra/benchmarks',
    'finatra/http:test-deps',
    'finatra/inject/inject-server:test-deps'
  ],
  fatal_warnings=True,
  sources=rglobs('*.scala'),
)
